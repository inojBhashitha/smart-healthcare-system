import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // Brand — Vivid, premium blue accent
  static const Color primary   = Color(0xFF3B82F6); // Vivid blue
  static const Color secondary = Color(0xFF06B6D4); // Cyan accent

  // Status
  static const Color success = Color(0xFF10B981); // Emerald
  static const Color warning = Color(0xFFF59E0B); // Amber
  static const Color danger  = Color(0xFFEF4444); // Red

  // Backgrounds — deep layered dark hierarchy
  static const Color background = Color(0xFF060D1A); // Near-black navy
  static const Color surface    = Color(0xFF0D1526); // Surface layer
  static const Color card       = Color(0xFF131F35); // Card layer
  static const Color cardElevated = Color(0xFF1A2A42); // Elevated card

  // Text
  static const Color textPrimary   = Color(0xFFF1F5F9);
  static const Color textSecondary = Color(0xFF8BA3C0);
  static const Color textDisabled  = Color(0xFF4A6080);

  // Others
  static const Color divider = Color(0xFF1E3250);

  // Accent tints for section cards
  static const Color accentBlue   = Color(0xFF1D4ED8);
  static const Color accentCyan   = Color(0xFF0891B2);
  static const Color accentPurple = Color(0xFF7C3AED);
  static const Color accentTeal   = Color(0xFF0D9488);
  static const Color accentGreen  = Color(0xFF16A34A);
}